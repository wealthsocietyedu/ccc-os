[phases.setup]
nixPkgs = ["ffmpeg", "yt-dlp", "python311"]
aptPkgs = ["fonts-dejavu-core"]

[phases.install]
cmds = [
  "pip3 install openai-whisper --break-system-packages || true"
]

[start]
cmd = "node server/index.js"